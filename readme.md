# MacOS preparation
```
brew install pyenv
pyenv install 3.12.13
cd /path/to/your/project
pyenv local 3.12.13
python --version
python -m venv .venv
source .venv/bin/activate
python --version
which python
```